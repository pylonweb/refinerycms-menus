Refinery::PageMenus.configure do |config|
  # Configure whether to allow adding new menus
  # config.new_menus = <% Refinery::PageMenus.new_menus %> 

	# Configure which menus that will be created when seeding. You can then call them by refinery_page_menu('MENU NAME')
  # config.default_menus = <% Refinery::PageMenus.default_menus %> 
  
  # Configure whether to have a overview page for all pages, or not (only pages for each menu)
  # config.pages_overview = <% Refinery::PageMenus.pages_overview %> 
  
  # Configure whether to have a collapsible menu or all menus on a list
  # config.collapsible_menu = <% Refinery::PageMenus.collapsible_menu %> 
  
  # Configure whether to hide hidden pages from the main menu tap. 
  # IMPORTANT! This can hide pages permanent if pages_overview is false! 
  # Always leave at least one of these options 'true'
  # config.show_hidden_pages_in_main_menu = <% Refinery::PageMenus.show_hidden_pages_in_main_menu %> 
end
